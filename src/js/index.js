// const result = 5 + 5 + "5";
// console.log(`result: ${result}.Тип змінной:${typeof result}`);

const email = "bodya@bodya.com";

if (email.includes("@")) {
  console.log("Email містить символ '@'");
} else {
  console.log("Email не містить символ '@'");
}

const length = email.length;
console.log(`Загальна кількість символів в email: ${length}`);
